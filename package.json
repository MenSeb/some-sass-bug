{
  "name": "some-sass-bug",
  "version": "1.0.0",
  "description": "",
  "main": "index.scss",
  "scripts": {
    "sass": "npx sass --pkg-importer=node --load-path=src --verbose",
    "sass:build": "npm run sass -- --no-source-map src:out",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MenSeb/some-sass-bug.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.77.8",
    "sass-true": "^8.0.0"
  }
}
